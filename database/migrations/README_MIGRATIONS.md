# 📋 คู่มือการรัน Migration Scripts

## ⚠️ สิ่งสำคัญ
**ไม่ต้องรัน scripts ทั้งหมด!** ให้รันเฉพาะ scripts ที่จำเป็นตามสถานะของ database ปัจจุบัน

---

## 📊 สรุป Scripts ทั้งหมด

### 1. ตาราง bmi_records (BMI Records)

#### ✅ `fix_bmi_records_table.sql` - **ควรรัน (ถ้ายังไม่มีตาราง)**
- **หน้าที่**: สร้างตาราง `bmi_records` พร้อม columns ทั้งหมด
- **เมื่อไหร่**: ถ้ายังไม่มีตาราง `bmi_records`
- **รวม**: `date`, `created_at`, `updated_at`

#### ⚠️ `add_created_at_to_bmi_records.sql` - **รันถ้าไม่มี column created_at**
- **หน้าที่**: เพิ่ม column `created_at` ถ้ายังไม่มี
- **เมื่อไหร่**: ถ้ามีตารางแล้วแต่ไม่มี `created_at`
- **หมายเหตุ**: แก้ไขแล้ว ไม่ใช้ `date` แล้ว (ปลอดภัย)

#### ⚠️ `add_date_column.sql` - **รันถ้าต้องการใช้ column date**
- **หน้าที่**: เพิ่ม column `date` ถ้ายังไม่มี
- **เมื่อไหร่**: ถ้าต้องการใช้ `date` column แยกจาก `created_at`

#### ✅ `fix_bmi_records_columns.sql` - **แนะนำ (ครอบคลุมที่สุด)**
- **หน้าที่**: เพิ่มทั้ง `created_at` และ `date` ถ้ายังไม่มี
- **เมื่อไหร่**: ใช้แทน scripts อื่นๆ สำหรับ bmi_records
- **ข้อดี**: ครอบคลุมทุกกรณี

---

### 2. ตาราง transaction_history (Transaction History)

#### ✅ `create_transaction_history_table.sql` - **ควรรัน**
- **หน้าที่**: สร้างตาราง `transaction_history`
- **เมื่อไหร่**: ถ้ายังไม่มีตารางนี้
- **สถานะ**: ✅ รันแล้ว (จากภาพที่เห็น)

---

### 3. ตาราง calorie_logs (Calorie Logs)

#### ✅ `create_calorie_logs_table.sql` - **ควรรัน**
- **หน้าที่**: สร้างตาราง `calorie_logs`
- **เมื่อไหร่**: ถ้ายังไม่มีตารางนี้
- **สำหรับ**: ระบบบันทึกข้อมูลแคลอรี่รายวัน

---

### 4. ตาราง cart_history (Cart History)

#### ✅ `create_cart_history_table.sql` - **ควรรัน**
- **หน้าที่**: สร้างตาราง `cart_history`
- **เมื่อไหร่**: ถ้ายังไม่มีตารางนี้
- **สำหรับ**: ระบบบันทึกประวัติตะกร้าสินค้า

---

## 🎯 แผนการรันที่แนะนำ

### สถานการณ์ที่ 1: Database ใหม่ (ยังไม่มีตารางเลย)

**รันตามลำดับ:**

1. ✅ `fix_bmi_records_table.sql` - สร้างตาราง bmi_records
2. ✅ `create_transaction_history_table.sql` - สร้างตาราง transaction_history
3. ✅ `create_calorie_logs_table.sql` - สร้างตาราง calorie_logs
4. ✅ `create_cart_history_table.sql` - สร้างตาราง cart_history

---

### สถานการณ์ที่ 2: มีตาราง bmi_records แล้ว แต่ไม่มี created_at

**รัน:**

1. ✅ `fix_bmi_records_columns.sql` - **แนะนำ (ครอบคลุมที่สุด)**
   - หรือ
2. ⚠️ `add_created_at_to_bmi_records.sql` - เพิ่มเฉพาะ created_at

---

### สถานการณ์ที่ 3: มีตาราง bmi_records แล้ว แต่ต้องการ column date

**รัน:**

1. ⚠️ `add_date_column.sql` - เพิ่ม column date

---

### สถานการณ์ที่ 4: มีตาราง bmi_records แล้ว แต่ต้องการทั้ง created_at และ date

**รัน:**

1. ✅ `fix_bmi_records_columns.sql` - **แนะนำ (ครอบคลุมที่สุด)**

---

## 📝 วิธีตรวจสอบว่าต้องรัน Script ไหน

### 1. ตรวจสอบตารางที่มีอยู่

รันใน Supabase SQL Editor:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

### 2. ตรวจสอบ columns ใน bmi_records

```sql
SELECT column_name, data_type 
FROM information_schema.columns
WHERE table_schema = 'public'
AND table_name = 'bmi_records'
ORDER BY ordinal_position;
```

### 3. ตรวจสอบว่ามี column created_at หรือไม่

```sql
SELECT EXISTS (
  SELECT FROM information_schema.columns
  WHERE table_schema = 'public'
  AND table_name = 'bmi_records'
  AND column_name = 'created_at'
) as has_created_at;
```

---

## ✅ Checklist สำหรับการรัน Scripts

### ตารางที่ต้องมี:

- [ ] `bmi_records` - ตาราง BMI
- [ ] `transaction_history` - ตารางประวัติการทำรายการ
- [ ] `calorie_logs` - ตารางบันทึกแคลอรี่
- [ ] `cart_history` - ตารางประวัติตะกร้า

### Columns ใน bmi_records ที่ต้องมี:

- [ ] `id` (UUID, Primary Key)
- [ ] `user_id` (UUID)
- [ ] `weight` (NUMERIC)
- [ ] `height` (NUMERIC)
- [ ] `bmi` (NUMERIC)
- [ ] `category` (VARCHAR)
- [ ] `calories` (NUMERIC, optional)
- [ ] `date` (TIMESTAMP, optional แต่แนะนำ)
- [ ] `created_at` (TIMESTAMP, **จำเป็น**)
- [ ] `updated_at` (TIMESTAMP, optional)

---

## 🚀 แผนการรันที่แนะนำที่สุด

### สำหรับระบบที่ใช้งานอยู่แล้ว:

1. ✅ **`fix_bmi_records_columns.sql`** - แก้ไข bmi_records ให้มี columns ครบ
2. ✅ **`create_transaction_history_table.sql`** - สร้างตาราง transaction_history (ถ้ายังไม่มี)
3. ✅ **`create_calorie_logs_table.sql`** - สร้างตาราง calorie_logs (ถ้ายังไม่มี)
4. ✅ **`create_cart_history_table.sql`** - สร้างตาราง cart_history (ถ้ายังไม่มี)

---

## ⚠️ หมายเหตุสำคัญ

1. **Scripts ปลอดภัย**: ทุก script ใช้ `IF NOT EXISTS` ดังนั้นรันซ้ำได้
2. **ไม่ทับข้อมูล**: Scripts ไม่ลบข้อมูลเดิม
3. **ตรวจสอบก่อนรัน**: ตรวจสอบว่ามีตาราง/columns อยู่แล้วหรือไม่
4. **ลำดับไม่สำคัญ**: Scripts แต่ละตัวเป็นอิสระจากกัน (ยกเว้น bmi_records scripts)

---

## 📌 สรุป

**ไม่ต้องรันทั้งหมด!** ให้รันเฉพาะ:

1. ✅ Scripts สำหรับตารางใหม่ (transaction_history, calorie_logs, cart_history)
2. ✅ Script แก้ไข bmi_records (เลือก 1 ตัว: `fix_bmi_records_columns.sql` แนะนำ)

**รวมแล้วประมาณ 4 scripts** (ไม่ใช่ 7 scripts ทั้งหมด)


